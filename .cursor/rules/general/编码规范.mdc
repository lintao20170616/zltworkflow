---
description: "通用编码规范，适用于所有项目和团队"
alwaysApply: true
tags: ["编码规范", "通用", "基础"]
version: "1.0.0"
---

# 通用编码规范

## AI助手行为规范
1. 使用简洁明了的中文进行交流
2. 优先使用中文注释和文档
3. 解释技术概念时使用通俗易懂的语言
4. 生成的代码必须包含必要的注释
5. 函数和类的职责要单一明确
6. 优先使用现代语言特性和最佳实践
7. 检查代码是否符合团队编码规范
8. 识别潜在的性能和安全问题
9. 确保代码的可维护性和可读性
10. 每次回复都要叫一声“领导”

## 编码规范
1. 使用英文命名，杜绝拼音和中文
2. 变量名使用驼峰命名法：`userName`
3. 常量名使用大写下划线：`MAX_RETRY_COUNT`
4. 函数名使用动词开头：`getUserInfo()`
5. 使用4个空格缩进（非制表符）
6. 行长度不超过120字符
7. 适当使用空行分隔逻辑块
8. 每个函数都需要文档注释
9. 复杂逻辑需要行内注释说明
10. TODO注释必须包含负责人和截止时间
11. 所有复杂的业务逻辑，不要加校验或异常捕获，只编写核心逻辑代码并注释说明；
12. 需要校验或异常捕获的代码，添加注释提示开发需要补充；

## 团队约定
1. 提交信息使用中文，格式：`类型: 描述`
2. 类型包括：`feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`
3. 主分支：`main` 或 `master`
4. 开发分支：`dev` 或 `develop`
5. 功能分支：`feature/功能名`
6. 修复分支：`fix/问题描述`

## 例外情况
1. 对于无法立即重构的遗留代码，可以适当放宽规范要求
2. 必须在TODO注释中标记重构计划
3. 在性能关键路径上，可以适当违反某些编码规范
4. 必须在注释中说明性能优化的原因
