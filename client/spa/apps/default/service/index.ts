import * as auth from './auth';
import * as chatbot from './chatbot';
import * as system from './system';
import * as menu from './menu';
import * as language from './language';

export { auth, chatbot, system, menu, language };
export default {
  auth,
  chatbot,
  system,
  menu,
  language,
};
